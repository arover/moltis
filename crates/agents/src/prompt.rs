/// Build the system prompt for an agent run.
pub fn build_system_prompt(_agent_id: &str, _config: &serde_json::Value) -> String {
    todo!("assemble system prompt from agent config, skills, and context")
}
