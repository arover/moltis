/// Config schema types (agents, channels, tools, session, gateway, plugins).
/// Corresponds to src/config/types.ts and zod-schema.*.ts in the TS codebase.
pub struct MoltisConfig {
    // TODO: define typed config sections
}
