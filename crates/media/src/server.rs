/// HTTP server for serving media files to agents.
pub async fn start_media_server(_media_dir: &std::path::Path, _port: u16) -> anyhow::Result<()> {
    todo!("serve files from media directory via axum")
}
